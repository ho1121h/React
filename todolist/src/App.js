import React from 'react';
import Todolist from './Todolist'; // Todolist 컴포넌트를 import 합니다. 경로는 실제 파일 위치에 따라 다를 수 있습니다.

function App() {
  return (
    <div className="App">
      <Todolist /> {/* Todolist 컴포넌트를 사용합니다. */}
    </div>
  );
}

export default App;
